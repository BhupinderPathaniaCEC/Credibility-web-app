@* Required by ASP.NET Core Identity default UI.
   This project uses API + Identity UI minimally, so
   we keep this partial empty on purpose. *@

@* You can later add login/logout links here if you
   integrate server-side layout/navigation. *@
